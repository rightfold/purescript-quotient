{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^12.4.0",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.6.0"
  }
}
